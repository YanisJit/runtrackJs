function citation() {
    // On récupère le texte contenu dans l'élément qui a l'id "citation"
    let texte = document.getElementById("citation").textContent;
    
    // On l'affiche dans la console
    console.log(texte);
}